crossplane:
  image:
    repository: '{{ .toptout.gar-proxy.hub_registry | "docker.io" }}/crossplane/crossplane'
  
job:
  image: '{{ .toptout.gar-proxy.hub_registry | "docker.io" }}/entigolabs/entigo-infralib-base'

imageConfigs:
  proxy: '{{ .toptout.gar-proxy.hub_registry | "docker.io" }}/entigolabs'
