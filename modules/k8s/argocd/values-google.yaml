global:
  cloudProvider: google

argocd:
  configs:
    params:
      server.insecure: true
  server:
    ingress:
      enabled: false
    httproute:
      # google has no ingress, the gateway route is always on
      enabled: true
